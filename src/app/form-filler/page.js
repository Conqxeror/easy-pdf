import FormFillerClient from "./components/FormFillerClient";

export const metadata = {
  title: "PDF Form Filler | Easy PDF",
  description: "Fill out PDF forms online for free. Add text, checkmarks, and signatures to any PDF document securely in your browser.",
};

export default function FormFillerPage() {
  return <FormFillerClient />;
}
